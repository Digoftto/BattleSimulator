# Battle Simulator Claude MVP Setup

Place these files in the Battle Simulator project:

- `CLAUDE.md` → project root
- `PROJECT_INDEX.md` → project root
- `.claude/skills/` → project root

The architecture documents referenced by the Index/Skills must remain in the project documentation locations defined by `PROJECT_INDEX.md`.

The Skills are routing/context layers. The architecture documents remain the source of truth.
